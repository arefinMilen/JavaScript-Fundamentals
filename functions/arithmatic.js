function math(x,y) {
    const sum = x + y;
    const difference = x - y;
    const product = sum * difference;
    const quotient = product / 2;
    return quotient;
}
const result = math(10, 5);
console.log("Result:", result);